version: '2'

linters:
  enable:
    - durationcheck
    - errcheck
    - forcetypeassert
    - ineffassign
    - makezero
    - misspell
    - nilerr
    - predeclared
    - staticcheck
    - unconvert
    - unparam
    - unused
    - govet

formatters:
  enable:
    - gofmt

run:
  go: '1.22' 
