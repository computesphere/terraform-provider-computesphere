---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "computesphere_api_token Resource - computesphere"
subcategory: ""
description: |-
  Manages an API token resource.
---

# computesphere_api_token (Resource)

Manages an API token resource.

## Example Usage

```terraform
terraform {
  required_providers {
    computesphere = {
      source = "computesphere.com/computesphere/computesphere"
    }
  }
}

variable "api_token" {
  description = "API token for ComputeSphere"
  type        = string
  sensitive   = true
}

variable "account_id" {
  description = "Account ID for ComputeSphere"
  type        = string
}

variable "api_url" {
  description = "API URL for ComputeSphere"
  type        = string
  default     = "api.computesphere.com"
}

provider "computesphere" {
  api_token  = var.api_token  # or set COMPUTESPHERE_API_TOKEN env variable
  account_id = var.account_id # or set COMPUTESPHERE_ACCOUNT_ID env variable
  api_url    = var.api_url    # or set COMPUTESPHERE_API_URL env variable
}

resource "computesphere_api_token" "example" {
  name  = "example-token"
  scope = "read"
}

output "api_token_id" {
  value = computesphere_api_token.example.id
}

output "api_token_name" {
  value = computesphere_api_token.example.name
}

output "api_token_scope" {
  value = computesphere_api_token.example.scope
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the API token.
- `scope` (String) Scope of the API token.

### Optional

- `account_id` (String) Account ID associated with the API token.
- `account_ids` (List of String) List of account IDs associated with the API token.
- `expiry` (String) Expiry date of the API token.
- `project_ids` (List of String) List of project IDs associated with the API token.
- `type` (String) Type of the API token.

### Read-Only

- `accounts` (List of String) List of account IDs associated with the API token.
- `created_at` (String) Creation timestamp of the API token.
- `id` (String) Unique identifier for the API token.
- `projects` (List of String) List of project IDs associated with the API token.
- `token` (String, Sensitive) The API token value.
- `user_id` (String) User ID associated with the API token.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to       = computesphere_api_token.example
  identity = { id = "c1d2e3f4-5678-90ab-cdef-1234567890ab" }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `id` (String)

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = computesphere_api_token.example
  id = "c1d2e3f4-5678-90ab-cdef-1234567890ab"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an API token by its ID
terraform import computesphere_api_token.example c1d2e3f4-5678-90ab-cdef-1234567890ab
```
